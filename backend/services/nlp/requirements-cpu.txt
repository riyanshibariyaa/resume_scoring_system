# NLP Extraction Service - CPU-Optimized Requirements (No GPU)
# Optimized for 4GB RAM, Windows development
# This skips heavy transformers and uses only lightweight spaCy

# Core Dependencies
Flask==3.0.0
flask-cors==4.0.0
python-dotenv==1.0.0
requests==2.31.0

# NLP - Lightweight (Only ~50MB total)
spacy==3.7.2
# Run after install: python -m spacy download en_core_web_sm

# Text Processing
nltk==3.8.1
regex==2023.10.3
dateparser==1.2.0
phonenumbers==8.13.26

# Data Handling
pandas==2.1.3
numpy==1.26.2

# ML - Lightweight
scikit-learn==1.3.2

# Utilities
werkzeug==3.0.1
loguru==0.7.2

# Note: Transformers/PyTorch are SKIPPED for faster setup
# The system will work with spaCy alone for development
